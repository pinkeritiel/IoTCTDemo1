services:
- module: pip-install
  packages:
    - locust==1.5.3

modules:
  pip-install:
    temp: false
    
execution:
- executor: locust
  concurrency: 10
  iterations: 15
  scenario: example

scenarios:
  example:
    default-address: https://www.demoblaze.com/
    script: testEntries.py